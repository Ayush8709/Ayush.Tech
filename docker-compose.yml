version: '3.8'

services:
  portfolio:
    image: ayush8709/portfolio:v1.0
    container_name: portfolio
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true